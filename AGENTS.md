# Repository Instructions

- Every codebase change must include a web app version bump in `web/package.json` and the root package entries in `web/package-lock.json`.
- Use SemVer patch bumps for normal fixes and UI changes, unless the requested change clearly calls for a minor or major version bump.
